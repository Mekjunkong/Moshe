---
description: v2.0.0 L-CMD | Find projects across git history, repos, docs, and Oracle. Use when user asks "trace", "find project", "where is [project]", "search history". Supports --oracle (fast), --smart (default), --deep (5 subagents). For session mining use /dig.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /trace

Execute the `trace` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "trace"` instead of reading the file manually.

**Otherwise**: Read the skill file at `/Users/pasuthunjunkong/workspace/Moshe/.opencode/skills/trace/SKILL.md` and follow ALL instructions in it.

---
*oracle-skills-cli v2.0.0*
