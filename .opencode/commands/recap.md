---
description: v2.0.0 L-CMD | Session orientation and awareness. Use when starting a session, after /jump, lost your place, switching context, or when user asks "now", "where are we", "what are we doing", "status".
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /recap

Execute the `recap` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "recap"` instead of reading the file manually.

**Otherwise**: Read the skill file at `/Users/pasuthunjunkong/workspace/Moshe/.opencode/skills/recap/SKILL.md` and follow ALL instructions in it.

---
*oracle-skills-cli v2.0.0*
